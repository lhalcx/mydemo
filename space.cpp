#include <iostream>

using namespace std;

int main(){
	char a,b,c;
	cin >> a >> b >> c;
	//cin.get(a);
	//cin.get(b);
	//cin.get(c);
	cout << (int)a << endl;
	cout << (int)b << endl;
	cout << (int)c << endl;
	return 0;
} 
